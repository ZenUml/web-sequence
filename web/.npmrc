# web/ is a self-contained project, intentionally isolated from the legacy
# pnpm workspace at the repo root (which only sets minimumReleaseAge). This keeps
# the rewrite's dependency tree independent until cutover (Milestone 05).
ignore-workspace=true
