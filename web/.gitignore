node_modules/
dist/
coverage/
*.local
.DS_Store
# M05: extension build artifacts (assembled dir + zip) — produced by build:extension.
extension/
extension.zip
# Playwright e2e run artifacts (the specs themselves under e2e/ ARE tracked).
test-results/
playwright-report/
.playwright/
storybook-static

# design-sync staged scripts + build output
.ds-sync/
ds-bundle/
